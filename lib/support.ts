/**
 * MVP placeholder — the founder's personal inbox, not a support alias.
 * Shared by PaymentPendingState.tsx and the purchase-confirmation email so
 * there's exactly one place to update. See docs/known_issues.md — to be
 * replaced with a proper support alias before wider public launch.
 */
export const SUPPORT_EMAIL = "onuohafrankline99@gmail.com";
